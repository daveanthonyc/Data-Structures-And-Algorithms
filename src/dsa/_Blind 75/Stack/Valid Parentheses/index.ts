/*
* Given a string s containing just the characters:
*   '(', ')', '{', '}', '[' and ']', 
*   determine if the input string is valid.
* 
* Example:
* () is valid, ((()() is not and returns true/false
 */

export default function isValid(str: string): boolean {
    return true;
}
